# Data Mining Tasks

- **information_gain**: A code written in Python that takes a dataset and builds a decision tree using the Information Gain method and displays it in the output (graphically and text-based)
  

Each task folder contains the Python script and a short usage guide.